* Para instalar docker compose: sudo curl -L "https://github.com/docker/compose/releases/download/1.26.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose.
* Para darle permisos de ejecución: sudo chmod +x /usr/local/bin/docker-compose.
* Para probarlo: docker-compose.
